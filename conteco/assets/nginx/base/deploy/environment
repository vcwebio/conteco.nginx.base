#!/usr/bin/env bash

# this file contains the default settings
# scaling parameters are applied after loading defaults

# GLOBAL REPLACEMENTS
DOLLAR='$'
export CONTECO_DC_GLOBAL='$CONTECO_DC_GLOBAL_SERVICE_NAME,$CONTECO_DC_GLOBAL_IMAGE_NAME'
export CONTECO_DC_GLOBAL_SERVICE_NAME="${CONTECO_TYPE}"
export CONTECO_DC_GLOBAL_IMAGE_NAME="${CONTECO_TYPE}.${CONTECO_NAME}:${CONTECO_TAG}"

# START VARIABLES nginx.base
export CONTECO_DC_NGINX_ENVIRONMENT=
export CONTECO_DC_NGINX_COMMAND=
export CONTECO_DC_NGINX_VOLUMES=
export CONTECO_DC_NGINX_PORTS="80:80"
export CONTECO_DC_NGINX_NETWORKS="${CONTECO_ECOSYSTEM}_overlay"
export CONTECO_DC_NGINX_DEPLOY=
# END VARIABLES nginx.base
